# 设计思路

> 本文是 [README](README.md) 第一节的展开版：逐条标注当前状态（已实现 / 协议已实现但闭环未跑完 / 实验过 / 尚未开始），并补充边界说明。
> 这份文档只写「为什么这么做」，不含任何素材、权重、客户信息与具体数值。
> 每条后面标注当前状态：已实现 / 协议已实现但闭环未跑完 / 实验过 / 尚未开始。

## 0. 一句话

把「模型会不会漏检」这种不可证的命题，换成「这条视频在声明的复核范围内，可读残留为零，且证据可复核」这种可执行门禁。

## 1. 可证伪的交付口径（已实现）

不承诺模型永不漏检。承诺的是：**对某个具体视频、在完整复核范围内，不存在可读车牌残留**。

- 没有素材覆盖的场景一律标「未验证」，不做外推；
- 结论绑定素材摘要，换视频即失效；
- 把不可证命题换成可执行门禁，是这套流程的地基。

## 2. fail-closed 生命周期（已实现并有回归）

输入冻结副本 → 排他 lease → 生成 canonical draft → 三类 failure marker → **原子发布**。

- 产物文件存在 == 本次通过；证据不跨轮复用；
- 双遍校验：源像素 / 遮挡后 / 编码再解码的帧链一致；
- 清理失败也算阻断——「守不住就丢弃」，不输出未打码帧。

> 实现见 [`fail_closed.py`](fail_closed.py)：`FrozenInputSet` / `OutputLease` /
> `render_masked_draft` + `two_pass_issues` / `publish_canonical_draft`，
> 三类 failure marker 为冻结副本清理、lease 释放、草稿清理。

## 3. 输出侧独立审计：按车辆轨迹，而不是帧（已实现）

审计器**不信任检测、跟踪和渲染**：编码完成后重新解码，逐车辆轨迹要求「可解释牌区」。

- 车辆框 ≠ 牌框，两者不能互相顶替；
- 一条轨迹找不到解释 → 记 UNKNOWN，**永久阻断**直到有人处理；
- 把漏检的计量单位从「帧 / 框」换成「车辆轨迹」：一辆车在整段视频里必须全程有解释。这是整套流程里最关键的判定口径改变。

> 实现见 [`fail_closed.py`](fail_closed.py)：`audit_decoded_output` 重新解码成品，
> 逐车辆轨迹调用 `explainable_plate_region`——既要求「有属于这辆车的计划遮挡」，
> 也要求「车辆框内没有未被计划覆盖的亮且带字符纹理的牌区」，缺一即 UNKNOWN。
> 只有绑定问题摘要的真人裁决（`validate_safe_issue_ids`）能豁免轨迹问题。

## 4. 证据权限模型：学生 → 教师 → 真人（协议已实现，闭环未跑完）

- **学生**（检测 / 跟踪）只产候选；
- **教师**（开放词表检测 / 小 VLM / 分割）只能**增加证据**，不能删除证据；多教师保守汇总——**共同阴性不等于安全**；
- **真人**只裁分歧与精确框；
- 结论值四态，UNKNOWN 永久阻断；两阶段确认：先对协议，再 attestation，且结论必须绑定内容摘要。

## 5. 候选并集与预注册门禁（已实现）

- 引擎不可被静默关闭；阈值有上限、切片 / ROI 有下限；
- 拥挤帧不得静默跳过车辆；
- 前提是把「引擎沉默」和「没有车牌」当成两件事：前者是风险，后者才配叫结论。

> 实现见 [`fail_closed.py`](fail_closed.py)：`validate_plan_bundle` 把畸形的候选输出、
> 帧数不符以及「一条车辆轨迹都没有」都记成不可豁免的阻断项（`coverage:*`），
> 阈值只作为可配置参数进入报告，不允许用「没跑 / 没出框」冒充阴性结论。

## 6. 时序冗余与传播停止条件（已实现）

- 两级关联 + 匀速预测 + **用最早的速度反推** + 同车锚点约束；
- 停止条件明确：至少 2 个锚点、传播有上限、硬切 / 离画即停；
- 宁可多打，也不允许把「预测不出来」当成「不用打」。

## 7. 评测方法论（工具已实现，GT 未构建）

- **可读性 GT 与几何 GT 分离**：能读出号码 ≠ 框得准，两件事分开评；
- 按**父视频**隔离划分，避免同源素材互相泄漏；
- 公开数据集在权利链没有确定之前不入库；
- 缺模型 / 没有输出**不算阴性**——不能拿「没跑」当「没检出」。

## 8. 教师链的负结果（实验过，同样值得写）

- 小 VLM 能判断「是否可读」，但它给出的框不能直接用于打码；且约 26 秒 / 帧，只适合高风险代表帧的复核；
- 开放词表教师低阈值枚举会引入语义误报，需要一层消误报；
- 分割教师因权重授权问题阻塞；
- 把教师伪标签蒸馏到端侧小模型这条路线：**用教师标注数据跑过 4 轮微调，指标不足以支撑换模型，正式权重未替换**——状态是「实验过、未采用」，不是「尚未开始」。

## 9. 边界（写清楚，避免误读）

- 本仓库不含素材、权重与客户信息；第三方引擎只做引用、按需惰性加载；
- 审计能证明「计划框已变成纯色」，**不能**证明画面中不存在未被计划的漏检车牌——正因如此才有第 3 节的轨迹级审计与 UNKNOWN 阻断；
- 这是一套可复核的工程流程，不是「模型很强」的声明。
